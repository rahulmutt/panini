# √bhuj and 1.3.66 — closing rudhādi at twenty-five — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Curate √bhuj (`07.0017`), rudhādi's last root, by implementing 1.3.66 *bhujo'navane* as an unconditional ubhayapada assignment with *anavane* recorded as an unimplemented sense restriction — taking the gaṇa to 25 of 25.

**Architecture:** No new phonology — 7f's probe certified that vidyut derives all 72 of √bhuj's cells on rules this engine already has, plus exactly one it lacks: 1.3.66, a root-keyed pada sanction. The root-keying never enters a rule: a new `PadaAssignment::UbhayapadaAnavane` on the data row maps to a new `Tag::Anavane` at `derive`'s one tagging site, and rule 1.3.66 in `samjna.rs` is a structural twin of 1.3.72 — sanction the ātmanepada reading, **decline** (never block) the parasmaipada one, which 1.3.78's śeṣa sanctions. The engine change is inert on the existing corpus **by construction** (both the new rule and 1.3.78's widened decline are guarded on a tag no existing row sets), so no dump-diff gate is needed; the full-corpus audit at the end re-verifies every cell anyway. Rules land before the row so no commit ever has a curated root whose ātmanepada cells block.

**Tech Stack:** Rust 1.98.0 pinned via `mise`; `cargo test` for the golden suite; `cargo-mutants` for the mutation gate; `vidyut-prakriya` (external, via the committed harness at `tools/audit/`) for the cross-implementation audit.

**Spec:** `docs/superpowers/specs/2026-08-26-rudhadi-bhuj-1-3-66-design.md` (committed to `main` as `3fe13fe`).

**Branch:** create `rudhadi-bhuj-1-3-66` from `main` before Task 1; all implementation commits go on it.

## Global Constraints

- **Toolchain is pinned to rust 1.98.0** via `mise.toml`. Use `mise run <task>` or `mise exec -- cargo …`; never install Rust globally. `mise run test -- -p X` does **not** scope to a package — use `mise exec -- cargo test -p X`.
- **`mise run lint` is `cargo clippy --workspace --all-targets -- -D warnings`, and `dead_code` is a warning.** Run `lint` only at the end of a task, never between adding a tag and adding the rule that reads it.
- **Run the golden suite in the FOREGROUND with an explicit long timeout.** The uncontended floor was 1066.83s (~18 min) at 2772 cells and grows to ~2844 cells in Task 5; budget **25 minutes** per `mise run test`. Do not background it and do not end a turn while it runs; a backgrounded suite gets orphaned and its result is lost.
- **`mise run mutants` is `-j 4 --timeout 4800`.** Run the task; do not reconstruct the flags. If the mise shim errors with `no version set for shim: cargo-mutants`, invoke the `cargo-mutants` binary directly with those same flags. `cargo-mutants` also reads `-j` from `CARGO_MUTANTS_JOBS`, so the environment can defeat the cap. (The spec's own "-j 4 --timeout 2400" states the *floor* requirement; the standing 4800 cap satisfies it with more headroom and is what `mise.toml` pins.)
- **Goldens are generated, never hand-authored.** Every `PARADIGM` block and `ALTERNATES` row comes out of the throwaway generator in Task 5, run against the engine the audit certified. Surfaces quoted in this document exist to make a wrong result recognisable; they are **not** to be typed into a test.
- **The audit's negative controls run first.** A zero-difference result recorded without a verified-failing control proves nothing.
- **`tools/audit/panini_full_audit.rs` is copied, never rewritten.** It is committed precisely so no slice reconstructs it.
- **SLP1 throughout.** `B` is bh, `Y` palatal ñ, `N` velar ṅ, `D` dh, `T` th, `E` ai. √bhuj's stored code is `Buj`; its upadeśa is `Bu\ja~`, whose `\` sits on the **root vowel** and says nothing about pada.

## Numbers this slice changes

Old values, for the arithmetic in Tasks 3, 4 and 5. Every one is asserted somewhere and will fail loudly if missed.

| quantity | old | new |
|---|---|---|
| `dhatus().len()` | 66 | **67** |
| `PARADIGM.len()` (blocks) | 308 | **316** |
| cells (`PARADIGM.len() * 9`) | 2772 | **2844** |
| `ALTERNATES.len()` | 487 | **494** |
| forms (cells + alternates) | 3259 | **3338** |
| rudhādi curated roots | 24 | **25** |
| rudhādi entries still out | 1 | **0** |
| roots that admit both padas | 11 | **12** |
| `TINANTA_RULES` (flattened) | 92 | **93** |
| pada-collision surfaces | 26 | **28** |

Cell-multiplicity buckets in `derivation_set_shape_matches_the_audited_numbers`, old → new: ones **2426 → 2493**, twos **247 → 250**, threes **81 → 83**, fours/fives/sixes **unchanged (2 / 8 / 8)**.

`ALTERNATES` key counts, old → new: `8.4.56` **111 → 114**, `7.1.35` **90 → 92**, `7.1.35+8.4.56` **90 → 92**; every other key unchanged.

Those "new" columns are **expectations to recognise a wrong result by**, not values to type in ahead of measurement. They are well-founded: √bhuj is √yuj (`07.0007`) with a `B` for its `y`, and √yuj contributes exactly 7 `ALTERNATES` rows — `8.4.56` ×3 (laṅ P cells 0 and 3, vidhiliṅ P cell 0), `7.1.35` ×2 and `7.1.35+8.4.56` ×2 (loṭ P cells 0 and 3) — all parasmaipada, none in ātmanepada (7.1.35 *tuhyos tātaṅ* is parasmaipada-only) and none from 8.4.65 (a velar junction is never savarṇa with a dental). Tasks 4 and 5 still measure rather than assume.

## What √bhuj's paradigm looks like

**Projected by `y` → `B` substitution from √yuj's audited blocks** — not probed cell-by-cell; 7f's design probe certified the totals (72 cells, 79 forms) and the rule set (1.3.66 the only gap). The audit in Task 4 is the authority. **Do not type these into a test.**

Parasmaipada:

| lakāra | the nine cells |
|---|---|
| laṭ | `Bunakti` `BuNktaH` `BuYjanti` `Bunakzi` `BuNkTaH` `BuNkTa` `Bunajmi` `BuYjvaH` `BuYjmaH` |
| laṅ | `aBunag`/`aBunak` `aBuNktAm` `aBuYjan` `aBunag`/`aBunak` `aBuNktam` `aBuNkta` `aBunajam` `aBuYjva` `aBuYjma` |
| loṭ | `Bunaktu`/`BuNktAd`/`BuNktAt` `BuNktAm` `BuYjantu` `BuNgDi`/`BuNktAd`/`BuNktAt` `BuNktam` `BuNkta` `BunajAni` `BunajAva` `BunajAma` |
| vidhiliṅ | `BuYjyAd`/`BuYjyAt` `BuYjyAtAm` `BuYjyuH` `BuYjyAH` `BuYjyAtam` `BuYjyAta` `BuYjyAm` `BuYjyAva` `BuYjyAma` |

Ātmanepada:

| lakāra | the nine cells |
|---|---|
| laṭ | `BuNkte` `BuYjAte` `BuYjate` `BuNkze` `BuYjATe` `BuNgDve` `BuYje` `BuYjvahe` `BuYjmahe` |
| laṅ | `aBuNkta` `aBuYjAtAm` `aBuYjata` `aBuNkTAH` `aBuYjATAm` `aBuNgDvam` `aBuYji` `aBuYjvahi` `aBuYjmahi` |
| loṭ | `BuNktAm` `BuYjAtAm` `BuYjatAm` `BuNkzva` `BuYjATAm` `BuNgDvam` `BunajE` `BunajAvahE` `BunajAmahE` |
| vidhiliṅ | `BuYjIta` `BuYjIyAtAm` `BuYjIran` `BuYjITAH` `BuYjIyATAm` `BuYjIDvam` `BuYjIya` `BuYjIvahi` `BuYjImahi` |

72 cells, 79 forms, 7 `ALTERNATES` rows expected — distributed 67 one-form cells, 3 two-form, 2 three-form. Expected new pada-collision surfaces: `BuNktAm` (P loṭ 1 vs A loṭ 0) and `aBuNkta` (P laṅ 5 vs A laṅ 0), the same shape as √yuj's `yuNktAm`/`ayuNkta` pair.

## File Structure

| file | responsibility | task |
|---|---|---|
| `crates/panini-data/src/lib.rs` | `PadaAssignment::UbhayapadaAnavane`, `padas()` arm, two variant tests extended | 1 |
| `crates/panini-prakriya/src/term.rs` | `Tag::Anavane` and its doc comment | 1 |
| `crates/panini-prakriya/src/tinanta/mod.rs` | the `UbhayapadaAnavane => Anavane` tagging arm | 1 |
| `crates/panini-prakriya/src/tinanta/samjna.rs` | `pada_anga`'s new match arm | 1 |
| `crates/panini-prakriya/src/tinanta/samjna.rs` | rule 1.3.66; 1.3.78's widened decline; module header; four guard tests | 2 |
| `crates/panini-prakriya/src/tinanta/derivation_tests.rs` | `tinanta_rule_order_is_pinned` gains `1.3.66` | 2 |
| `crates/panini-data/src/lib.rs` | the √bhuj row; census renamed to twenty-five; the consistency-test exception; prose counts | 3 |
| `crates/panini-prakriya/src/tinanta/samjna.rs` | order-independence extended to four rules; `07.0017` in the 1.3.72-declines list | 3 |
| `crates/panini-prakriya/src/tinanta/derivation_tests.rs` | `bhuj_derives_in_both_padas` | 3 |
| `crates/panini/tests/paradigm.rs` | `GATED` (8 triples), then `PARADIGM`, `ALTERNATES`, the audited-numbers test, its doc comment, the pada-collision vec | 3, 5 |
| `tools/audit/panini_full_audit.rs`, `tools/audit/README.md` | corpus totals; "Last recorded result" | 4 |
| `crates/panini/tests/trace.rs` | the two new pins | 6 |
| `AGENTS.md` | the mutation paragraph | 7 |
| `README.md`, `docs/ARCHITECTURE.md`, `AGENTS.md`, `crates/panini-data/src/lib.rs` | prose, counts, recorded results | 8 |

---

### Task 1: The variant and the tag

**Files:**
- Modify: `crates/panini-data/src/lib.rs:24-39` (`PadaAssignment`, `padas()`), `:1062-1081` (the two variant tests)
- Modify: `crates/panini-prakriya/src/term.rs:35` (after `Ubhayapadin`)
- Modify: `crates/panini-prakriya/src/tinanta/mod.rs:84-88` (the tagging match)
- Modify: `crates/panini-prakriya/src/tinanta/samjna.rs:214-223` (`pada_anga`)

**Interfaces:**
- Consumes: nothing.
- Produces: `PadaAssignment::UbhayapadaAnavane` (data layer) and `Tag::Anavane` (engine), plus the mapping between them, which Tasks 2 and 3 build on. `UbhayapadaAnavane.padas()` returns `&[Pada::Parasmaipada, Pada::Atmanepada]`, parasmaipada first.

- [ ] **Step 1: Extend the two variant tests so they fail to compile**

In `crates/panini-data/src/lib.rs`, in `padas_maps_each_assignment_to_its_derivable_padas`, append after the `Ubhayapada` assertion:

```rust
        assert_eq!(
            PadaAssignment::UbhayapadaAnavane.padas(),
            &[Pada::Parasmaipada, Pada::Atmanepada]
        );
```

and in `ubhayapada_padas_are_parasmaipada_first`, append:

```rust
        assert_eq!(
            PadaAssignment::UbhayapadaAnavane.padas()[0],
            Pada::Parasmaipada
        );
```

- [ ] **Step 2: Run to verify the failure**

Run: `mise exec -- cargo test -p panini-data padas`
Expected: FAIL to compile — `no variant named UbhayapadaAnavane`.

- [ ] **Step 3: Add the variant and its `padas()` arm**

In `crates/panini-data/src/lib.rs`, extend the enum:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum PadaAssignment {
    Parasmaipada,
    Atmanepada,
    Ubhayapada,
    /// Both padas derive, exactly as for `Ubhayapada` — but the ātmanepada
    /// arm is sanctioned by 1.3.66 *bhujo'navane*, a sūtra that names the
    /// root, rather than by a svarita/ñit marker read through 1.3.72. The
    /// distinction is what the TRACE needs: a root carrying this assignment
    /// must never reach 1.3.72, or the derivation credits the wrong sūtra.
    ///
    /// 1.3.66 restricts ātmanepada to senses other than protecting
    /// (*anavane*). Neither this engine nor vidyut-prakriya models sense,
    /// so the assignment is UNCONDITIONAL — both readings derive, the
    /// parasmaipada one by 1.3.78's śeṣa (the *avane* reading), and the
    /// reader selects by sense. Same precedent as 1.3.72's own unmodelled
    /// *kartrabhiprāye kriyāphale*; see the 1.3.66 comment block in
    /// panini-prakriya's `tinanta/samjna.rs`.
    UbhayapadaAnavane,
}
```

and in `padas()`, replace the `Ubhayapada` arm with:

```rust
            PadaAssignment::Ubhayapada | PadaAssignment::UbhayapadaAnavane => {
                &[Pada::Parasmaipada, Pada::Atmanepada]
            }
```

- [ ] **Step 4: Add `Tag::Anavane`**

In `crates/panini-prakriya/src/term.rs`, immediately after the `Ubhayapadin` variant:

```rust
    /// The dhatu is the root 1.3.66 bhujo'navane names -- today exactly
    /// √bhuj (`07.0017`). Its ātmanepada is sanctioned by that root-keyed
    /// sūtra (in senses other than protecting, a restriction recorded but
    /// not modelled -- see the 1.3.66 comment block in `tinanta/samjna.rs`),
    /// its parasmaipada by 1.3.78's śeṣa.
    ///
    /// Deliberately distinct from Ubhayapadin even though the derivational
    /// behavior is identical: the tag keys the TRACE. Tagged Ubhayapadin,
    /// √bhuj would reach 1.3.72 and the trace would credit the wrong sūtra
    /// -- the mirror image of the √indh counterexample above, where a
    /// marker-named tag would have fired 1.3.72 on a root whose pada
    /// 1.3.12 had already settled. Read only by 1.3.66, and by 1.3.78's
    /// ātmanepada arm, which declines rather than blocks when it is
    /// present. Pinned by `svaritanit_declines_the_root_1_3_66_names`.
    Anavane,
```

- [ ] **Step 5: Add the tagging arm and the `pada_anga` arm**

In `crates/panini-prakriya/src/tinanta/mod.rs`, the `match dhatu.pada` gains one arm after `Ubhayapada`:

```rust
            PadaAssignment::UbhayapadaAnavane => t.add(Tag::Anavane),
```

In `crates/panini-prakriya/src/tinanta/samjna.rs`, `pada_anga`'s match gains the same arm (the match is deliberately exhaustive — this is the compile error that forced the edit):

```rust
            PadaAssignment::UbhayapadaAnavane => t.add(Tag::Anavane),
```

- [ ] **Step 6: Run the workspace tests and lint**

Run: `mise exec -- cargo test -p panini-data && mise exec -- cargo test -p panini-prakriya && mise run lint && mise run fmt-check`
Expected: all pass. No behavior changes yet — no row carries the variant, no rule reads the tag. (`Tag::Anavane` is constructed by the `mod.rs` arm and `PadaAssignment::UbhayapadaAnavane` is `pub`, so `dead_code` stays quiet.)

- [ ] **Step 7: Commit**

```bash
git add crates/panini-data/src/lib.rs crates/panini-prakriya/src/term.rs crates/panini-prakriya/src/tinanta/mod.rs crates/panini-prakriya/src/tinanta/samjna.rs
git commit -m "feat(data): PadaAssignment::UbhayapadaAnavane and Tag::Anavane

The 1.3.66 keying mechanism: a root-keyed pada fact stated on the data
row, mapped to a tag at derive's one tagging site, so no rule ever reads
a root identity. No row carries it yet and no rule reads it yet; both
arrive in the next two tasks."
```

---

### Task 2: 1.3.66, and 1.3.78's widened decline

**Files:**
- Modify: `crates/panini-prakriya/src/tinanta/samjna.rs` (module header `:1`; new `Rule` after 1.3.12's closing `},` at `:45`; 1.3.78's ātmanepada arm at `:107-113`; four new tests)
- Modify: `crates/panini-prakriya/src/tinanta/derivation_tests.rs:122` (`tinanta_rule_order_is_pinned`)

**Interfaces:**
- Consumes: `Tag::Anavane` from Task 1.
- Produces: rule `"1.3.66"` (name `"Bujo'navane"`, `RuleKind::Vidhi`, `vikalpa: false`), which sanctions the ātmanepada reading of an `Anavane`-tagged aṅga and declines its parasmaipada reading; 1.3.78 declining (not blocking) an `Anavane`-tagged ātmanepada request. Task 3's row and Task 6's pins rely on exactly this trace behavior.

- [ ] **Step 1: Write the failing rule-order pin**

In `crates/panini-prakriya/src/tinanta/derivation_tests.rs`, in `tinanta_rule_order_is_pinned`, change the head of the expected array from

```rust
        "1.3.12", "1.3.72", "1.3.78",
```

to

```rust
        "1.3.12", "1.3.66", "1.3.72", "1.3.78",
```

- [ ] **Step 2: Run it to verify it fails**

Run: `mise exec -- cargo test -p panini-prakriya tinanta_rule_order_is_pinned`
Expected: FAIL — the actual sequence has no `1.3.66`.

- [ ] **Step 3: Add the rule**

In `crates/panini-prakriya/src/tinanta/samjna.rs`, between 1.3.12's closing `},` and the comment block above 1.3.72, insert:

```rust
    // 1.3.66 Bujo'navane: √bhuj takes ātmanepada in senses other than
    // protecting (avane); in the protecting sense it falls to 1.3.78's
    // śeṣa. Root-keyed — the sūtra names the root — which is why the guard
    // is Tag::Anavane, the data layer's UbhayapadaAnavane, rather than any
    // marker: `Bu\ja~` carries no pada anubandha at all
    // (`curated_pada_agrees_with_upadesha_markers` asserts that from the
    // vendored upadeśa).
    //
    // THE SENSE RESTRICTION IS NOT MODELLED, and that is deliberate — the
    // exact precedent 1.3.72 sets for *kartrabhiprāye kriyāphale*
    // immediately below. Both arms derive, each trace crediting the sūtra
    // that sanctioned it, and the reader selects by sense. NOT vikalpa,
    // for 1.3.72's reason verbatim: pada is a context coordinate, so the
    // two readings are two CELLS, not two branches of one cell.
    //
    // The parasmaipada arm DECLINES rather than blocks — 1.3.78 sanctions
    // it, and blocking here would collapse the ubhayapada behavior this
    // rule exists to open. Structural twin of 1.3.72 in every clause; only
    // the guard tag and the credited sūtra differ.
    Rule {
        id: "1.3.66",
        name: "Bujo'navane",
        kind: RuleKind::Vidhi,
        vikalpa: false,
        apply: |p| {
            // svarita/ñit ubhayapadī roots are 1.3.72's business;
            // parasmaipada-only roots 1.3.78's; ātmanepada-only ones
            // 1.3.12's.
            if !p.terms[ANGA].has(Tag::Anavane) {
                return false;
            }
            match p.ctx.pada {
                Pada::Atmanepada => {
                    let before = p.snapshot();
                    p.record("1.3.66", "Bujo'navane", before);
                    true
                }
                Pada::Parasmaipada => false,
            }
        },
    },
```

- [ ] **Step 4: Widen 1.3.78's decline**

In 1.3.78's `Pada::Atmanepada` arm, replace

```rust
                Pada::Atmanepada => {
                    if p.terms[ANGA].has(Tag::Ubhayapadin) {
                        return false;
                    }
                    p.blocked = true;
                    false
                }
```

with

```rust
                Pada::Atmanepada => {
                    if p.terms[ANGA].has(Tag::Ubhayapadin) || p.terms[ANGA].has(Tag::Anavane) {
                        return false;
                    }
                    p.blocked = true;
                    false
                }
```

and extend the comment above that arm — "1.3.72 has already sanctioned this cell, so decline instead of blocking" — to say "1.3.72 (Ubhayapadin) or 1.3.66 (Anavane) has already sanctioned this cell, so decline instead of blocking. Only the genuine śeṣa (no pada tag at all) blocks here."

Update the module header's first line to:

```rust
//! Saṃjñā, pada sanction and ending insertion: 1.3.12, 1.3.66, 1.3.72,
//! 1.3.78, 3.4.78, 1.3.9, 1.2.4.
```

- [ ] **Step 5: Write the four guard tests**

In `samjna.rs`'s test module, after `pada_prakriya`, add a hand-built counterpart — √bhuj's row does not land until Task 3, and the rule reads only the tag:

```rust
    /// `pada_prakriya` for the root 1.3.66 names, hand-built: √bhuj's
    /// `Dhatu` row lands in the data task after this one, and the rule
    /// reads only `Tag::Anavane`, so the term is constructed directly
    /// rather than looked up in `dhatus()`.
    fn anavane_prakriya(pada: Pada) -> Prakriya {
        let mut t = Term::new("Buj");
        t.add(Tag::Dhatu);
        t.add(Tag::Anavane);
        let mut p = Prakriya {
            ctx: Context::new(Lakara::Lat, pada, Purusha::Prathama, Vacana::Eka),
            ..Default::default()
        };
        p.terms.push(t);
        p
    }
```

Then the four tests, mirroring the existing 1.3.72 pins:

```rust
    #[test]
    fn bhujo_anavane_reports_firing_only_on_atmanepada() {
        // Same shape as `svaritanit_reports_firing_only_on_atmanepada`:
        // 1.3.66 sanctions the ātmanepada reading and DECLINES the
        // parasmaipada one — declines, not blocks: 1.3.78 is what
        // sanctions parasmaipada (the avane reading), and blocking here
        // would collapse the ubhayapada behavior this rule exists to open.
        let rule = rules().find(|r| r.id == "1.3.66").unwrap();
        for (pada, fires) in [(Pada::Atmanepada, true), (Pada::Parasmaipada, false)] {
            let mut p = anavane_prakriya(pada);
            assert_eq!((rule.apply)(&mut p), fires, "1.3.66 on {pada:?}");
            assert!(!p.blocked, "1.3.66 must never block, {pada:?}");
        }
    }

    #[test]
    fn bhujo_anavane_declines_for_roots_it_does_not_name() {
        // The guard is Tag::Anavane and nothing else. A svarita/ñit
        // ubhayapadī root (√rudh) is 1.3.72's business, a parasmaipada-only
        // root (√bhū, untagged) 1.3.78's, and an ātmanepada-only one
        // (√khid) 1.3.12's; 1.3.66 must leave all three alone in both
        // padas, without recording and without blocking.
        let rule = rules().find(|r| r.id == "1.3.66").unwrap();
        for number in ["07.0001", "01.0001", "07.0012"] {
            let code = pada_anga_text(number);
            for pada in [Pada::Parasmaipada, Pada::Atmanepada] {
                let mut p = pada_prakriya(number, pada);
                assert!(!(rule.apply)(&mut p), "1.3.66 fired on {code} {pada:?}");
                assert!(!p.blocked, "1.3.66 blocked {code} {pada:?}");
                assert!(p.log.is_empty(), "1.3.66 recorded on {code} {pada:?}");
            }
        }
    }

    #[test]
    fn svaritanit_declines_the_root_1_3_66_names() {
        // The wrong-sūtra-credit case, pinned: 1.3.72 reads Ubhayapadin
        // and must never fire on an Anavane-tagged aṅga — were √bhuj to
        // reach it, the trace would credit a svarita/ñit sanction the
        // root's upadeśa does not carry. The mirror image of the √indh
        // counterexample in Tag::Ubhayapadin's doc comment.
        let rule = rules().find(|r| r.id == "1.3.72").unwrap();
        for pada in [Pada::Parasmaipada, Pada::Atmanepada] {
            let mut p = anavane_prakriya(pada);
            assert!(!(rule.apply)(&mut p), "1.3.72 fired on Buj {pada:?}");
            assert!(!p.blocked, "1.3.72 blocked Buj {pada:?}");
            assert!(p.log.is_empty(), "1.3.72 recorded on Buj {pada:?}");
        }
    }

    #[test]
    fn shesat_declines_the_root_1_3_66_names() {
        // 1.3.78's ātmanepada arm must DECLINE an Anavane-tagged request
        // — 1.3.66 has sanctioned that cell — while its parasmaipada arm
        // still fires: the śeṣa (the avane reading) is 1.3.78's own.
        let rule = rules().find(|r| r.id == "1.3.78").unwrap();
        let mut p = anavane_prakriya(Pada::Atmanepada);
        assert!(!(rule.apply)(&mut p), "1.3.78 fired on Buj Atmanepada");
        assert!(!p.blocked, "1.3.78 blocked Buj Atmanepada");
        let mut p = anavane_prakriya(Pada::Parasmaipada);
        assert!((rule.apply)(&mut p), "1.3.78 declined Buj Parasmaipada");
        assert!(!p.blocked);
    }
```

- [ ] **Step 6: Run the crate's tests, then the full suite in the foreground**

Run: `mise exec -- cargo test -p panini-prakriya`
Expected: all pass, including the rule-order pin at 93 and the four new tests.

Run: `mise run test && mise run lint && mise run fmt-check`
Expected: all pass — 1.3.66 declines on every existing root (no row sets the tag), so every golden and every full-order trace pin is untouched. A trace-pin failure here means the guard is wrong, not the pin.

- [ ] **Step 7: Commit**

```bash
git add crates/panini-prakriya/src/tinanta/samjna.rs crates/panini-prakriya/src/tinanta/derivation_tests.rs
git commit -m "feat(samjna): 1.3.66 Bujo'navane, a structural twin of 1.3.72

Sanctions the ātmanepada reading of an Anavane-tagged root and declines
the parasmaipada one, which 1.3.78's śeṣa sanctions — its ātmanepada arm
now declines on Anavane as well as Ubhayapadin. The anavane sense
restriction is recorded, not modelled, on 1.3.72's own precedent. Inert
by construction on the existing corpus: no curated row sets the tag yet."
```

---

### Task 3: The √bhuj row

**Files:**
- Modify: `crates/panini-data/src/lib.rs` (the row after `07.0008`; `rudhadi_rows_…`; `curated_pada_agrees_with_upadesha_markers`; `dhatus().len()`; the prose counts at `:83`, `:94`, `:1184`)
- Modify: `crates/panini-prakriya/src/tinanta/samjna.rs` (order-independence to four rules; `07.0017` in the 1.3.72-declines list)
- Modify: `crates/panini-prakriya/src/tinanta/derivation_tests.rs` (`bhuj_derives_in_both_padas`)
- Modify: `crates/panini/tests/paradigm.rs:5960` (`GATED`)

**Interfaces:**
- Consumes: the rule and widened 1.3.78 from Task 2 (without them the row's ātmanepada cells block, and `bhuj_derives_in_both_padas` is the pin on that).
- Produces: `07.0017` as a curated `Dhatu` row with `PadaAssignment::UbhayapadaAnavane`, which Tasks 4, 5 and 6 all enumerate over.

- [ ] **Step 1: Write the failing table test**

In `crates/panini-data/src/lib.rs`, rename `rudhadi_rows_are_the_twenty_four_curated_roots` to `rudhadi_rows_are_the_twenty_five_curated_roots` and append one entry to its expected vec, after `("07.0008", "Cfd", PadaAssignment::Ubhayapada),`:

```rust
                ("07.0017", "Buj", PadaAssignment::UbhayapadaAnavane),
```

Replace the test's final comment paragraph — the one beginning "ONE of rudhādi's 25 is still out after this" — with:

```rust
        // The √bhuj slice then closed the gaṇa at TWENTY-FIVE OF
        // TWENTY-FIVE: √bhuj (`07.0017`) needed no phonology at all — it
        // is √yuj with a `B` — and exactly one rule, 1.3.66 Bujo'navane, a
        // root-keyed pada assignment structurally identical to 1.3.72's.
        // 1.3.66 restricts ātmanepada to senses other than protecting, and
        // neither engine models sense, so the row ships an UNCONDITIONAL
        // ubhayapada assignment (PadaAssignment::UbhayapadaAnavane) with
        // anavane recorded as unimplemented — the same precedent 1.3.72
        // set for kartrabhiprAye kriyAPale. No rudhādi entry remains out.
```

- [ ] **Step 2: Run it to verify it fails**

Run: `mise exec -- cargo test -p panini-data rudhadi_rows_are_the_twenty_five_curated_roots`
Expected: FAIL — `assertion failed: (left == right)`, the left vec missing the new triple.

- [ ] **Step 3: Add the `Dhatu` row**

In `crates/panini-data/src/lib.rs`, after the `07.0008` row's closing `},` and before the array's closing `];`:

```rust
    Dhatu {
        // 07.0017 Bu\ja~ pAlanAByavahArayoH. The row that closes rudhādi
        // at 25 of 25. Its `\` sits on the ROOT VOWEL — no pada anubandha
        // at all — so the markers say parasmaipada, and the ātmanepada arm
        // comes from 1.3.66 Bujo'navane, the one curated verdict a sūtra
        // carries rather than a marker; UbhayapadaAnavane is that fact
        // stated as data, and `curated_pada_agrees_with_upadesha_markers`
        // asserts both sides of the divergence explicitly.
        //
        // No new phonology: √bhuj is √yuj (`07.0007`) with a `B` for its
        // `y` — śnam, 8.2.30's kutva on the final `j`, the same 8.4.56
        // and 7.1.35 forks, and no 8.4.65 anywhere (a velar junction is
        // never savarṇa with a dental). 7f's design probe certified 1.3.66
        // as the only rule vidyut invokes for it that this engine lacked.
        dhatupatha: "07.0017",
        code: "Buj",
        gana: Gana::Rudhadi,
        pada: PadaAssignment::UbhayapadaAnavane,
        artha: "pAlanAByavahArayoH",
    },
```

- [ ] **Step 4: Add the consistency-test exception**

In `curated_pada_agrees_with_upadesha_markers`, immediately after `let derived = pada_from_upadesha(upadesha);`, insert:

```rust
            // 1.3.66 Bujo'navane: the one curated verdict no marker can
            // carry. `Bu\ja~` has no pada anubandha at all — its `\` sits
            // on the root vowel, the exact conflation this function's doc
            // comment warns against — so the markers CORRECTLY derive
            // parasmaipada, and the ātmanepada arm comes from a sūtra that
            // names the root, which is why Pāṇini needed 1.3.66 in the
            // first place. Both sides are asserted so this exception fails
            // loudly if either the upadeśa reading or the curated column
            // ever drifts.
            if d.dhatupatha == "07.0017" {
                assert_eq!(
                    derived,
                    PadaAssignment::Parasmaipada,
                    "Bu\\ja~ grew a pada marker; 1.3.66's exception is stale"
                );
                assert_eq!(
                    d.pada,
                    PadaAssignment::UbhayapadaAnavane,
                    "07.0017 is the root 1.3.66 names; its pada is curated, not marker-derived"
                );
                continue;
            }
```

- [ ] **Step 5: Bump `dhatus().len()` and fix the count-bearing comments**

- In `curated_roots_have_expected_ganas_and_padas` (`:804`): `assert_eq!(dhatus().len(), 67);`
- `Dhatu::pada`'s doc comment (`:82-83`): "re-derives every one of these 66 verdicts from the vendored upadeśa via 1.3.12 / 1.3.72 / 1.3.78 and requires it to match" → "re-derives 66 of these 67 verdicts from the vendored upadeśa via 1.3.12 / 1.3.72 / 1.3.78 and requires them to match; the 67th, √bhuj's, is 1.3.66's root-keyed exception, asserted explicitly from both sides".
- `:94`: "The test covers the 66 roots curated here" → **67**.
- `pada_from_upadesha`'s doc comment (`:1184`): "43 of the 66 curated roots carry a `\` at all, and 30 of those carry one on a root vowel" → "**44 of the 67** … and **31** of those" (`Bu\ja~`'s `\` is on the root vowel).

- [ ] **Step 6: Run the data crate's tests**

Run: `mise exec -- cargo test -p panini-data`
Expected: all pass, including the renamed census and the exception — which now exercises both its assertions.

- [ ] **Step 7: Extend the samjna order-independence test to four rules**

In `crates/panini-prakriya/src/tinanta/samjna.rs`, in `pada_sutras_are_order_independent`: extend the framing comment's disjointness argument with one sentence — "1.3.66 joins the family with the same disjointness: Anavane is exclusive with the other tags on the root, and where it overlaps 1.3.78 (an Anavane root is `!Atmanepadin`) they split on `ctx.pada` exactly as 1.3.72 does." — then replace the constants:

```rust
        const IDS: [&str; 4] = ["1.3.12", "1.3.66", "1.3.72", "1.3.78"];
        const ORDERS: [[usize; 4]; 24] = [
            [0, 1, 2, 3],
            [0, 1, 3, 2],
            [0, 2, 1, 3],
            [0, 2, 3, 1],
            [0, 3, 1, 2],
            [0, 3, 2, 1],
            [1, 0, 2, 3],
            [1, 0, 3, 2],
            [1, 2, 0, 3],
            [1, 2, 3, 0],
            [1, 3, 0, 2],
            [1, 3, 2, 0],
            [2, 0, 1, 3],
            [2, 0, 3, 1],
            [2, 1, 0, 3],
            [2, 1, 3, 0],
            [2, 3, 0, 1],
            [2, 3, 1, 0],
            [3, 0, 1, 2],
            [3, 0, 2, 1],
            [3, 1, 0, 2],
            [3, 1, 2, 0],
            [3, 2, 0, 1],
            [3, 2, 1, 0],
        ];
```

and append two cells to the `cells` array:

```rust
            ("07.0017", Pada::Parasmaipada, Some("1.3.78")),
            ("07.0017", Pada::Atmanepada, Some("1.3.66")),
        ];
```

Also add `"07.0017"` to the loop list in `svaritanit_declines_for_roots_that_are_not_ubhayapadin` — `for number in ["01.0001", "07.0012", "07.0011", "07.0017"]` — and extend that test's comment: "…and the root 1.3.66 names (√bhuj, Anavane-tagged) must never reach 1.3.72 either — the dhatus()-backed twin of `svaritanit_declines_the_root_1_3_66_names`."

- [ ] **Step 8: Add the end-to-end pin**

In `crates/panini-prakriya/src/tinanta/derivation_tests.rs`, after `rudh_derives_in_both_padas`:

```rust
#[test]
fn bhuj_derives_in_both_padas() {
    // The end-to-end pin on derive's UbhayapadaAnavane → Anavane tagging
    // arm, mirroring `rudh_derives_in_both_padas` for the Ubhayapada arm:
    // with that arm missing, the ātmanepada cell would block. The trace
    // must credit 1.3.66 — not 1.3.72, which √bhuj can never reach —
    // for the ātmanepada cell, and 1.3.78's śeṣa for the parasmaipada one.
    let bhuj = dhatus().iter().find(|d| d.dhatupatha == "07.0017").unwrap();

    let p = sole(derive(
        bhuj,
        Lakara::Lat,
        Pada::Parasmaipada,
        Purusha::Prathama,
        Vacana::Eka,
    ));
    assert!(!p.blocked, "the root 1.3.66 names must derive in parasmaipada");
    assert_eq!(p.text(), "Bunakti");
    assert_eq!(p.log.first().unwrap().sutra, "1.3.78");

    let p = sole(derive(
        bhuj,
        Lakara::Lat,
        Pada::Atmanepada,
        Purusha::Prathama,
        Vacana::Eka,
    ));
    assert!(!p.blocked, "the root 1.3.66 names must derive in ātmanepada");
    assert_eq!(p.text(), "BuNkte");
    assert_eq!(p.log.first().unwrap().sutra, "1.3.66");
}
```

- [ ] **Step 9: Gate the eight golden triples**

The root is curated but has no `PARADIGM` blocks until Task 5. In `crates/panini/tests/paradigm.rs`, replace `const GATED: &[(&str, &str, Pada)] = &[];` with:

```rust
    const GATED: &[(&str, &str, Pada)] = &[
        ("07.0017", "laT", Pada::Parasmaipada),
        ("07.0017", "laT", Pada::Atmanepada),
        ("07.0017", "laN", Pada::Parasmaipada),
        ("07.0017", "laN", Pada::Atmanepada),
        ("07.0017", "loT", Pada::Parasmaipada),
        ("07.0017", "loT", Pada::Atmanepada),
        ("07.0017", "viDiliN", Pada::Parasmaipada),
        ("07.0017", "viDiliN", Pada::Atmanepada),
    ];
```

The lakāra strings above match the existing `PARADIGM` blocks (`"laT"`, `"laN"`, `"loT"`, `"viDiliN"`); confirm against any existing root's four blocks before relying on them.

- [ ] **Step 10: Run the full golden suite in the foreground**

Run: `mise run test`
Expected: all pass (budget ~25 minutes; run in the foreground). `paradigm_covers_every_enumerable_cell` is satisfied by `GATED`; `derivation_set_shape_matches_the_audited_numbers` still asserts the **old** 2772/487 numbers and still passes, because `PARADIGM` has not grown. `roundtrip` now derives √bhuj's 72 cells in both padas — if any ātmanepada cell blocks, Task 2's 1.3.78 widening is wrong.

- [ ] **Step 11: Commit**

```bash
git add crates/panini-data/src/lib.rs crates/panini-prakriya/src/tinanta/samjna.rs crates/panini-prakriya/src/tinanta/derivation_tests.rs crates/panini/tests/paradigm.rs
git commit -m "feat(data): Buj, rudhadi's twenty-fifth and last root

UbhayapadaAnavane on the row, 1.3.66 in the trace. The consistency test
gains its first documented exception, asserted from both sides: Bu\\ja~
has no pada anubandha, so the markers correctly say parasmaipada, and the
atmanepada arm is the sutra that names the root. Eight golden triples
gated until the audit certifies the derivations."
```

---

### Task 4: The cross-implementation audit — the blocking gate

**Files:**
- Modify: `tools/audit/panini_full_audit.rs` (the corpus-total assertions at `:577-580`, and the header's totals at `:12-13`, `:25-28`, `:55`)
- Modify: `tools/audit/README.md` (the asserted-totals line, "Last recorded result", and the growth arithmetic)

**Interfaces:**
- Consumes: the complete engine (Tasks 1, 2) and the row (Task 3). The harness enumerates `dhatus()` and `d.pada.padas()`, so √bhuj's two padas arrive with no harness logic change — only totals move.
- Produces: the verdict Task 5's goldens are generated under, and the measured form total `<N>` that Tasks 5 and 8 both quote.

- [ ] **Step 1: Update the harness's corpus totals**

In `tools/audit/panini_full_audit.rs`, at `:577-580`:

```rust
    assert_eq!(roots_seen.len(), 67, "curated roots");
    assert_eq!(n_cells, 2844, "cells: 316 root×pada×lakāra blocks × 9");
    assert_eq!(n_forms, 3338, "forms: 2844 cells + 494 ALTERNATES rows");
```

and update the header mentions — `:12` (`for each of the 66 curated roots` → 67), `:13` (`two apiece for the eleven ubhayapadī roots` → `two apiece for the twelve roots that admit both padas — eleven ubhayapadī by 1.3.72, plus √bhuj by 1.3.66`), `:25` (`66 roots, 2772 cells, 3259 forms` → `67 roots, 2844 cells, 3338 forms`), `:27-28` (`308 root×pada×lakāra blocks × 9 cells, plus 487 ALTERNATES rows` → `316 … plus 494 …`), `:55` (`the full 2772-cell table` → 2844).

If the run reports a different **form** total than 3338, **the harness's number is the measurement and 3338 was the projection** — change the assertion to what it measured and carry that value forward into Tasks 5 and 8. A differing **cell** count, by contrast, means a pada or lakāra is miscounted: 316 is `308 + 8`, and 8 is one root × two padas × four lakāras. Investigate rather than adjust.

- [ ] **Step 2: Verify the vidyut checkout and copy the harness in**

```bash
git -C /tmp/vidyut-full rev-parse HEAD
```

Expected: `8da2f90bee3ce1c07505fa432fc3729e3f7e02ea` — the audited commit every prior slice used. If the checkout is missing or at another commit, restore it per `tools/audit/README.md` before proceeding; do not audit against anything else.

```bash
cp tools/audit/panini_full_audit.rs /tmp/vidyut-full/vidyut-prakriya/examples/
```

- [ ] **Step 3: Run the `entry` negative control FIRST**

```bash
cd /tmp/vidyut-full/vidyut-prakriya && PANINI_AUDIT_PERTURB=entry cargo run --release --example panini_full_audit; echo "exit=$?"
```

Expected: **exit 1**, with 36 √bhū cells flagged. A zero-difference result recorded without this proves nothing.

- [ ] **Step 4: Run the `form` negative control**

```bash
cd /tmp/vidyut-full/vidyut-prakriya && PANINI_AUDIT_PERTURB=form cargo run --release --example panini_full_audit; echo "exit=$?"
```

Expected: **exit 1**.

- [ ] **Step 5: Run the real audit**

```bash
cd /tmp/vidyut-full/vidyut-prakriya && cargo run --release --example panini_full_audit; echo "exit=$?"
```

Expected: **exit 0**, 67 roots / 2844 cells / `<N>` forms, zero differences.

**If any cell differs, stop.** Tasks 1–2 are inert on every root but √bhuj by construction, so a difference belongs to √bhuj, and the failing cell names the cause:

- **An ātmanepada cell missing on this engine's side entirely** — 1.3.66 declined or 1.3.78 blocked: `Tag::Anavane` is not reaching the aṅga (the `mod.rs` arm), or 1.3.78's widened decline was mis-typed.
- **A parasmaipada cell missing** — 1.3.66's parasmaipada arm is blocking instead of declining, or 1.3.12's block is being reached; neither should be possible with the Task 2 guard tests green, so re-read them against the shipped code.
- **A surface differing on a cell both engines derive** — this is the serious one: it contradicts 7f's probe finding that √bhuj needs no phonology this engine lacks. Diff the vidyut trace for that cell against `TINANTA_RULES` and identify the missing or too-narrow rule; the fix is the rule, never the golden. The 8.2.30/8.2.39 slice is the precedent: its first audit found four differing cells, and that was the finding.

**What is NOT a difference, and must not be chased:** vidyut credits 6.1.68 for deleting laṅ's apṛkta `t`; this engine reaches the same surfaces through 8.2.23. That standing divergence is invisible to this harness (it compares derivation **sets**, not traces), holds across every rudhādi root, and √bhuj inherits it unchanged.

- [ ] **Step 6: Record the result**

In `tools/audit/README.md`, update the asserted-totals line to `(67 roots, 2844 cells, 3338 forms)` (or the measured `<N>`) and "Last recorded result" to name: 2026-08-26, the √bhuj/1.3.66 slice, vidyut `8da2f90bee3ce1c07505fa432fc3729e3f7e02ea`, 67 roots / 2844 cells / `<N>` forms, zero differences, both `entry` and `form` negative controls verified failing first, and the growth arithmetic — 66 → 67 roots (√bhuj, `07.0017`); 2772 → 2844 cells (72 = 1 root × 2 padas × 4 lakāras × 9); 3259 → `<N>` forms, `ALTERNATES` 487 → 494 (expected; Task 5 measures).

- [ ] **Step 7: Commit**

```bash
git add tools/audit/panini_full_audit.rs tools/audit/README.md
git commit -m "test(audit): Buj is byte-identical to vidyut across 2844 cells"
```

---

### Task 5: The goldens, generated

**Files:**
- Create then delete: `crates/panini/tests/print_bhuj_goldens.rs`
- Modify: `crates/panini/tests/paradigm.rs` (`PARADIGM`, `ALTERNATES`, `GATED` back to `&[]`, `derivation_set_shape_matches_the_audited_numbers` and its doc comment, the pada-collision vec and its comment)

**Interfaces:**
- Consumes: Task 4's certified engine.
- Produces: the 8 `PARADIGM` blocks and 7 `ALTERNATES` rows every later task's counts refer to.

- [ ] **Step 1: Write the throwaway generator**

Create `crates/panini/tests/print_bhuj_goldens.rs`:

```rust
//! THROWAWAY -- the Buj/1.3.66 slice only. Prints √bhuj's `PARADIGM`
//! blocks and `ALTERNATES` rows as Rust source, plus the distribution
//! counts `derivation_set_shape_matches_the_audited_numbers` asserts.
//! Deleted in the same task that pastes its output: goldens are generated
//! from the engine the audit certified, never hand-authored.
//!
//! Run with:
//!   mise exec -- cargo test -p panini --test print_bhuj_goldens -- --nocapture

mod common;

use common::{CELLS, LAKARA_BY_NAME};
use panini_data::{Pada, dhatus};
use panini_prakriya::derive;

const NEW_ROOTS: [&str; 1] = ["07.0017"];

/// Mirrors `VIKALPA_RULES` in `paradigm.rs`. An alternate's key is the
/// `+`-joined list of optional rules its branch actually applied, which is
/// what `every_alternate_names_the_vikalpa_rules_that_produced_it` checks.
/// This slice adds no optional rule, so the list is unchanged from 7f.
const VIKALPA_RULES: &[&str] = &[
    "7.1.35", "3.4.111", "6.4.107", "8.2.74", "8.2.75", "8.4.65", "8.4.56",
];

#[test]
fn print_bhuj_goldens() {
    // (rendered row, key) -- the key is kept alongside rather than parsed
    // back out of the rendered string.
    let mut alternates: Vec<(String, String)> = Vec::new();
    // Indexed by forms-per-cell. Sized well past the repo's deepest fork
    // (six) so an unexpectedly sharp cell prints instead of panicking.
    let mut multiplicity = [0usize; 12];

    println!("\n// ==== PARADIGM blocks ====");
    for number in NEW_ROOTS {
        let d = dhatus()
            .iter()
            .find(|d| d.dhatupatha == number)
            .unwrap_or_else(|| panic!("{number} is not a curated root"));
        for &pada in d.pada.padas() {
            for (lak_name, lak) in LAKARA_BY_NAME {
                let mut goldens: Vec<String> = Vec::new();
                for (cell, &(pu, va)) in CELLS.iter().enumerate() {
                    let branches = derive(d, lak, pada, pu, va);
                    // Index 0 is the declined derivation -- the one with no
                    // optional rule applied. `derivation_set_is_exactly_pinned`
                    // requires PARADIGM to hold exactly this.
                    let golden = branches[0].text();
                    let mut n_alts = 0usize;
                    for p in branches.iter().filter(|p| !p.blocked && p.text() != golden) {
                        let key: Vec<&str> = p
                            .log
                            .iter()
                            .map(|s| s.sutra.as_str())
                            .filter(|s| VIKALPA_RULES.contains(s))
                            .collect();
                        let key = key.join("+");
                        alternates.push((
                            format!(
                                "    ({:?}, {:?}, Pada::{:?}, {}, {:?}, {:?}),",
                                number,
                                lak_name,
                                pada,
                                cell,
                                p.text(),
                                key
                            ),
                            key,
                        ));
                        n_alts += 1;
                    }
                    multiplicity[n_alts + 1] += 1;
                    goldens.push(format!("{golden:?}"));
                }
                println!("    (");
                println!("        {number:?},");
                println!("        {lak_name:?},");
                println!("        Pada::{pada:?},");
                println!("        [");
                println!("            {},", goldens.join(", "));
                println!("        ],");
                println!("    ),");
            }
        }
    }

    println!("\n// ==== ALTERNATES rows ====");
    // Deduplicate: two branches can reach the same surface by different
    // optional-rule paths, and ALTERNATES holds one row per (cell, form).
    let mut seen = std::collections::BTreeSet::new();
    let mut per_key: std::collections::BTreeMap<String, usize> = Default::default();
    for (row, key) in &alternates {
        if seen.insert(row.clone()) {
            println!("{row}");
            *per_key.entry(key.clone()).or_default() += 1;
        }
    }

    println!("\n// ==== counts ====");
    println!("// new ALTERNATES rows: {}", seen.len());
    for (key, n) in &per_key {
        println!("// key {key:?}: +{n}");
    }
    for (n, count) in multiplicity.iter().enumerate() {
        if *count > 0 {
            println!("// cells with {n} form(s): +{count}");
        }
    }
}
```

- [ ] **Step 2: Run the generator**

Run: `mise exec -- cargo test -p panini --test print_bhuj_goldens -- --nocapture`

Expected: eight `PARADIGM` blocks, **7** `ALTERNATES` rows, and counts reading `key "8.4.56": +3`, `key "7.1.35": +2`, `key "7.1.35+8.4.56": +2`, `cells with 1 form(s): +67`, `cells with 2 form(s): +3`, `cells with 3 form(s): +2`.

That profile is exactly √yuj's. If the counts differ, the derivation differs from the audited one — which cannot happen, since Task 4 certified it. Re-run Task 4 rather than adjusting the numbers.

- [ ] **Step 3: Paste the blocks into `PARADIGM`**

Paste the eight printed blocks verbatim into `crates/panini/tests/paradigm.rs`'s `PARADIGM`, after `07.0008`'s blocks. Do not retype any surface.

- [ ] **Step 4: Paste the rows into `ALTERNATES`**

Paste the 7 printed rows verbatim into `ALTERNATES`, at the end.

- [ ] **Step 5: Empty `GATED`**

Restore `crates/panini/tests/paradigm.rs`'s `GATED` to:

```rust
    const GATED: &[(&str, &str, Pada)] = &[];
```

and add one sentence to the comment above it recording that √bhuj's eight triples were gated for one commit in the Buj/1.3.66 slice, between the row landing and the audited goldens arriving.

- [ ] **Step 6: Update the audited-numbers assertions**

In `derivation_set_shape_matches_the_audited_numbers`:

```rust
    let total_cells = PARADIGM.len() * 9;
    assert_eq!(total_cells, 2844, "316 root×lakāra blocks × 9 cells each");
```

```rust
    assert_eq!(ones, 2493, "one-form cells");
    assert_eq!(twos, 250, "two-form cells");
    assert_eq!(threes, 83, "three-form cells");
```

The `fours` (2), `fives` (8) and `sixes` (8) assertions are **unchanged**, message text included — √bhuj joins neither record.

```rust
    assert_eq!(ALTERNATES.len(), 494, "ALTERNATES row count");
```

and the key counts: `8.4.56` **114**, `7.1.35` **92**, `7.1.35+8.4.56` **92**. `8.4.65` (145), `7.1.35+8.4.65` (16), `7.1.35+8.4.65+8.4.56` (16), `8.2.75` (8), `6.4.107` (8), `3.4.111` (2) and `8.2.74` (1) are all unchanged.

If any pasted-and-counted number disagrees with this table, the generator's own printed counts are the measurement — reconcile against Task 4's audit before touching an assertion.

- [ ] **Step 7: Update that test's doc comment**

Three edits:

- The root roster line ("kft/his/Banj/piz/…/Cid/Cfd") gains `Buj`.
- After the √chid/√chṛd fork-record sentence, append:

```rust
/// √bhuj joins neither fork record, exactly as √yuj does not: its `j`
/// junctions are velar after 8.2.30's kutva, never savarṇa with a dental,
/// so 8.4.65 has nothing to elide anywhere in its paradigm — it forks
/// only on 8.4.56 (laṅ and vidhiliṅ finals) and 7.1.35 (loṭ tātaṅ),
/// seven ALTERNATES rows with √yuj's exact key profile.
```

- The audit-history sentence gains: "and the √bhuj/1.3.66 slice's cross-implementation audit re-ran the same probe against vidyut-prakriya at commit `8da2f90` over all 2844 cells / `<N>` forms / 67 roots with zero differences, its `entry` negative control verified failing" — with `<N>` from Task 4.

- [ ] **Step 8: Extend the pada-collision vec**

√bhuj contributes the same pair every j-final ubhayapada-deriving rudhādi root does — expected `"BuNktAm"` and `"aBuNkta"`, the shape of √yuj's `yuNktAm`/`ayuNkta`. Add to the vec, and add a sentence to the narrating comment: the √bhuj slice's one new both-pada root contributes two more, the same shape as √yuj's pair, taking the set to twenty-eight with no new collision against any pre-slice surface.

**Do not guess the sort position.** Run the test once and let its diff place them — the vec is sorted by the harness, and the assertion prints the expected ordering on failure.

- [ ] **Step 9: Delete the generator**

```bash
rm crates/panini/tests/print_bhuj_goldens.rs
```

- [ ] **Step 10: Run the full golden suite in the foreground**

Run: `mise run test`
Expected: all pass. This is the first run at 2844 cells; budget 25 minutes.

- [ ] **Step 11: Commit**

```bash
git add crates/panini/tests/paradigm.rs
git commit -m "test(paradigm): Buj's audited paradigm

Eight PARADIGM blocks and 7 ALTERNATES rows, generated from the engine
the audit certified. Buj is yuj with a B, and the numbers agree: the same
seven-row alternate profile (8.4.56 x3, 7.1.35 x2, 7.1.35+8.4.56 x2), no
8.4.65 anywhere, and the same two pada-collision surfaces."
```

---

### Task 6: The trace pins

**Files:**
- Modify: `crates/panini/tests/trace.rs`

**Interfaces:**
- Consumes: the curated root and its goldens.
- Produces: the two pada-sanction pins the spec names.

- [ ] **Step 1: Write the two tests**

Append to `crates/panini/tests/trace.rs`:

```rust
#[test]
fn bhunkte_trace_credits_1_3_66_not_1_3_72() {
    // Buj laT ātmanepada prathama eka — the slice's one new fact, read off
    // the trace. √bhuj's ātmanepada is sanctioned by the root-keyed 1.3.66
    // Bujo'navane, and the pada sanction opens every trace, so 1.3.66 is
    // at index 0. 1.3.72 must NOT appear: the root carries Tag::Anavane,
    // not Ubhayapadin, precisely so the trace cannot credit a svarita/ñit
    // sanction that `Bu\ja~`'s upadeśa does not carry. 1.3.78 must not
    // appear either — it declined this reading rather than blocking it.
    let (text, t) = cell_trace(
        "07.0017",
        Lakara::Lat,
        Pada::Atmanepada,
        Purusha::Prathama,
        Vacana::Eka,
    );
    assert_eq!(text, "BuNkte", "got {t:?}");
    assert_eq!(at(&t, "1.3.66"), 0, "the pada sanction opens the trace, got {t:?}");
    assert!(!t.contains(&"1.3.72".to_string()), "got {t:?}");
    assert!(!t.contains(&"1.3.78".to_string()), "got {t:?}");
}

#[test]
fn bhunakti_trace_credits_the_shesa_1_3_78() {
    // The arm an UNCONDITIONAL 1.3.66 must leave open: the engine models
    // no sense, so the avane reading derives too, sanctioned by 1.3.78's
    // śeṣa exactly as for 1.3.72's roots — which is what "1.3.66 declines
    // rather than blocks" buys. 1.3.66 must not appear in this trace: it
    // declined without recording.
    let (text, t) = cell_trace(
        "07.0017",
        Lakara::Lat,
        Pada::Parasmaipada,
        Purusha::Prathama,
        Vacana::Eka,
    );
    assert_eq!(text, "Bunakti", "got {t:?}");
    assert_eq!(at(&t, "1.3.78"), 0, "the pada sanction opens the trace, got {t:?}");
    assert!(!t.contains(&"1.3.66".to_string()), "got {t:?}");
    assert!(!t.contains(&"1.3.72".to_string()), "got {t:?}");
}
```

- [ ] **Step 2: Run them**

Run: `mise exec -- cargo test -p panini --test trace`
Expected: all pass, including the two new ones.

- [ ] **Step 3: Commit**

```bash
git add crates/panini/tests/trace.rs
git commit -m "test(trace): Buj's pada sanctions, read off the trace

BuNkte opens with 1.3.66 and never cites 1.3.72 or 1.3.78; Bunakti opens
with 1.3.78's shesa and never cites 1.3.66. Together they pin the
decline-don't-block contract from both sides."
```

---

### Task 7: The mutation gate

**Files:**
- Modify: `AGENTS.md` (the `cargo-mutants` paragraph)

**Interfaces:**
- Consumes: the finished engine and suite.
- Produces: the recorded floor, contention and campaign figures the next slice reasons from.

- [ ] **Step 1: Measure the uncontended floor**

Run: `mise run test` and record `paradigm`, `roundtrip` and `trace`'s individual times plus the wall clock.

The floor stood at **1066.834s** at 2772 cells (paradigm 486.69s, roundtrip 576.91s, trace 2.39s). Cell growth here is +2.6%. **Do not scale — measure.** Cell count has failed as a multiplier for six consecutive slices.

- [ ] **Step 2: Check the floor against the cap**

Multiply the measured floor by the **1.02×–1.43×** `-j 4` contention range. Against the 4800s cap the margin should remain around 3× or better. Do not quote the 1.02× end alone. If the projected worst case exceeds ~2400s, raise the cap in `mise.toml` and say so in the AGENTS.md entry rather than running under a thin margin.

- [ ] **Step 3: Run the campaign**

Run: `mise run mutants`

If the shim errors with `no version set for shim: cargo-mutants`, invoke the binary directly with the same flags:

```bash
mise exec -- cargo-mutants --package panini-prakriya --test-workspace=true --timeout 4800 -j 4
```

Run it in the **foreground**. Expect several hours (7f's campaign: 571 mutants, 10h wall).

- [ ] **Step 4: Check both files, not just `missed.txt`**

```bash
cat mutants.out/missed.txt; echo "--- timeouts ---"; cat mutants.out/timeout.txt
```

Expected, and **verify rather than assume**:

- **One timeout** — the known-permanent non-terminating-loop mutant on `tripadi.rs`'s 8.4.2 backward ṇatva scan (`j -= 1` → `j /= 1`, which makes `j` constant so the run never reaches an assertion). Identify it **by that shape**, not by line number. It is the correct verdict at any cap; do not chase it with a bigger `--timeout` or a code change.
- **Two missed**, and they must be the two verified equivalent mutants recorded since 7e: `adesha.rs`'s 6.1.87 im-arm `+`→`*` and `tripadi.rs`'s 8.3.13 guard mutant. Both are documented in place at their guards.

**A third missed mutant, or a different two, is a real finding.** Candidates given what this slice adds, and the test that should kill each: 1.3.66's guard deleted (fires on every ātmanepada cell — the existing full-order trace pins grow a spurious `1.3.66` entry); its `Atmanepada` arm inverted (√bhuj's ātmanepada cells block — `bhuj_derives_in_both_padas` and the goldens); 1.3.78's `||`→`&&` (√rudh's ātmanepada blocks — every ubhayapadī golden); the `mod.rs` `UbhayapadaAnavane` arm emptied (`bhuj_derives_in_both_padas`); `padas()`'s combined arm narrowed (`padas_maps_each_assignment_to_its_derivable_padas`, and `paradigm_covers_every_enumerable_cell`'s block count). If one of these survives, the named test is wrong — fix the test, not the census.

- [ ] **Step 5: Fix any genuine survivor**

Add the missing test, or delete the dead code the survivor exposes. Re-run only the affected mutants rather than the whole campaign.

- [ ] **Step 6: Record the numbers in `AGENTS.md`**

Append a paragraph to the `cargo-mutants` entry in the same voice as the existing ones, giving: the cell count (2844), the measured floor with its three component times, the growth against 7f's 1066.834s at 2772 cells and whether cell count predicted it, the cap sanity check and its basis, the campaign totals (mutants / caught / missed / unviable / timeout), the per-mutant test-phase spread with the over-600s count, and the ruling on the cap.

- [ ] **Step 7: Commit**

```bash
git add AGENTS.md mutants.out
git commit -m "test(mutants): record the Buj/1.3.66 campaign result"
```

---

### Task 8: The documentation sweep

**Files:**
- Modify: `AGENTS.md`, `docs/ARCHITECTURE.md`, `README.md`

**Interfaces:**
- Consumes: every measured number from Tasks 4, 5 and 7.
- Produces: a repo whose prose matches its assertions.

**A grep for the totals will not find them all.** Several are line-wrapped across two lines and several are rule-scoped rather than corpus-scoped. Work the list below file by file; do not substitute a grep for it. `docs/superpowers/` is never swept — past specs and plans are historical records.

- [ ] **Step 1: `AGENTS.md`**

- The golden-suite paragraph: `2772 cells` → 2844; `487 rows in all, so 2772 + 487 = 3259 forms total` → `494 rows in all, so 2844 + 494 = 3338`; the trace-pin root roster ("rudhādi's √kṛt, √rudh, … and — new in slice 7f — …") gains √bhuj's two pins; the fork census is **unchanged** (√bhuj joins neither record) — verify that sentence rather than editing it.
- The rudhādi paragraph (the one ending "24 curated + √bhuj = 25, so **1 of the 25 remains out**"): rewrite its √bhuj sentences to what is now true — the √bhuj slice curated it behind 1.3.66 *bhujo'navane*, implemented as an unconditional ubhayapada assignment (`PadaAssignment::UbhayapadaAnavane` → `Tag::Anavane`, so the trace credits 1.3.66 and can never reach 1.3.72) with *anavane* recorded as an unimplemented sense restriction on 1.3.72's own precedent; "**25 curated — no rudhādi entry remains out**". The follow-on sentence "The root count is not what keeps the gaṇa partial … what remains is √bhuj's sense axis" must go — the gaṇa is no longer partial. Say instead that rudhādi is the first gaṇa curated at its full dhātupāṭha strength.
- Any "eleven ubhayapadī roots" phrasing → "twelve roots that admit both padas (eleven ubhayapadī by 1.3.72, √bhuj by 1.3.66)".
- The audit paragraph: add this slice's run — 67 roots / 2844 cells / `<N>` forms at vidyut `8da2f90`, both negative controls verified failing first.

- [ ] **Step 2: `docs/ARCHITECTURE.md`**

- `:85`: "The gaṇa carries twenty-four roots" → twenty-five, appending √bhuj (`07.0017`), curated in the √bhuj/1.3.66 slice with 1.3.66 *bhujo'navane*, ubhayapada-deriving with *anavane* unmodelled.
- `:139`: "2772 cells / 3259 forms / 66 roots" → the new totals.
- `:152-163`: the paragraph beginning "The twenty-fifth, √bhuj (`07.0017`), is out on different grounds" must be rewritten — √bhuj is in. "**1 of the 25 in all**" → none; "rudhādi is already past it at twenty-four" → "rudhādi is complete at twenty-five".
- `:341`: "45 + 21 = the 66 curated" — **read the sentence first** to learn which buckets the two addends name (it predates this slice), then move √bhuj into the correct bucket and take the sum to 67. Do not pattern-match the arithmetic without reading it.

- [ ] **Step 3: `README.md`**

- `:18`: "*rudhādi* (7, śnam) **partial**: twenty-four" → complete: twenty-five (drop the **partial** marker; every listed gaṇa's phrasing is the model — match it).
- `:30-36`: the "**1 of the 25 remains out** — √bhuj, deferred behind 1.3.66 …" paragraph: replace with the curated statement — √bhuj is in, behind 1.3.66 *bhujo'navane* implemented as an unconditional ubhayapada assignment with the *anavane* sense restriction recorded as unimplemented (neither engine models sense), so both padas derive and the trace credits 1.3.66 for the ātmanepada cells and 1.3.78's śeṣa for the parasmaipada ones.
- `:38`: "over a curated 66-root set" → 67.
- The cell-multiplicity sentence ("… of the 2772 cells hold more than one form: 247 hold two, 81 hold three …"): 2844; "351 of the 2844 … 250 hold two, 83 hold three … two hold four … eight hold five, and eight hold six" (351 = 250 + 83 + 2 + 8 + 8 — recompute from Task 5's landed assertions, not from this document).

- [ ] **Step 4: Verify the sweep**

```bash
grep -rn "2772\|3259\|twenty-four\|1 of the 25\|\b487\b\|\b66 roots\b\|eleven ubhayapadī" --include=*.md --include=*.rs . | grep -v "^./target\|^./mutants.out\|docs/superpowers/"
```

Expected: only intentional historical references remain — AGENTS.md's mutation paragraphs legitimately cite 2772 and 1066.834s as *7f's* measurement, and `tools/audit/README.md`'s growth arithmetic legitimately cites the old totals as the "from" side. Anything else is a miss. Then hand-check the two spots a grep cannot match: line-wrapped counts in AGENTS.md, and the vikalpa-rule prose (the seven optional-rule ids are untouched by this slice, so any drift found there predates it — flag it, don't fold it in silently).

- [ ] **Step 5: Run the gates once more and commit**

Run: `mise run test && mise run lint && mise run fmt-check`

```bash
git add AGENTS.md README.md docs/ARCHITECTURE.md
git commit -m "docs: rudhadi is complete at twenty-five

Buj moves from the deferral notice to the curated enumeration in all
three front-door documents. The gana is the first curated at its full
dhatupatha strength; what 1.3.66 does not model (anavane) is recorded
where 1.3.72's own unmodelled condition is."
```

---

### Task 9: Finish the branch

**Files:** none.

**Interfaces:**
- Consumes: every prior task.
- Produces: the merge.

- [ ] **Step 1: Open the PR**

```bash
git push -u origin rudhadi-bhuj-1-3-66
gh pr create --fill
```

The PR body should state: the audit's verdict (vidyut commit, 67 roots / 2844 cells / `<N>` forms, zero differences, both negative controls verified failing first); the mutation result (mutants, caught, the two known equivalent missed, the one known-permanent timeout); and the one-sentence version of what the slice is — rudhādi closed at 25/25 by ruling on 1.3.66, not by phonology: an unconditional ubhayapada assignment whose trace credits the root-keyed sūtra, with *anavane* recorded as unimplemented on 1.3.72's precedent.

Append `https://claude.ai/code/session_01C1eU7JVg3Zu6LHHjBECbEj` to the body.

- [ ] **Step 2: Merge and clean up**

Use the `superpowers:finishing-a-development-branch` skill: wait for CI, merge the green PR, verify the commits are on `main`, then delete the branch and any worktree.

---

## Deferred, and why

- **Splitting `crates/panini/tests/paradigm.rs`** (past 6,000 lines after Task 5). Still its own slice, for 7e's standing reason, and the natural next item now that rudhādi is closed.
- **The other root-keyed pada sūtras** (1.3.38–43, 1.3.65, and the rest of 1.3.66's family), until a curated root needs one. `UbhayapadaAnavane` is the shape to generalize when one does — the next sūtra's condition may not be a sense restriction, so generalize then, not now.
- **8.4.41's correspondence side** for `d`/`n`/`s` and **8.4.42/8.4.43/8.4.44**: still unwitnessed, still out.
- **6.3.111** and **6.1.68**: both deliberately absent, both documented; √bhuj inherits the 6.1.68 divergence like every rudhādi root.
- **It-stripping as real sūtras** (1.3.2, 1.3.3, 1.3.5, 1.3.9).
